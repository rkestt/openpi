# ── Pi / Beads agent artifacts ─────────────────────────────────────────────
.beads/
.bv/
.pi/tasks/
.pi/tasks-config.json
.pi/task-registry.json
.pi/artifacts/

# ── Dependencies ───────────────────────────────────────────────────────────
node_modules/

# ── Build outputs ──────────────────────────────────────────────────────────
# electron-vite compiled output
out/
dist/
dist-electron/
# electron-builder packaged installers
release/
# CI release artifact download directory
dist-artifacts/
coverage/
*.tsbuildinfo

# ── electron-builder generated files ──────────────────────────────────────
# Debug/effective config generated by electron-builder
builder-debug.yml
builder-effective-config.yaml

# ── Test / browser automation ──────────────────────────────────────────────
playwright-report/
test-results/

# ── Runtime data ───────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.sqlite
*.sqlite-shm
*.sqlite-wal

# ── Code-signing certificates (never commit) ───────────────────────────────
*.pem
*.p12
*.p8
*.pfx

# ── OS metadata ────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ── Local environment ──────────────────────────────────────────────────────
.env
.env.*
!.env.example
.pi/extensions/harness/*.bak

# ── Local todos not tracked ────────────────────────────────────────────
*.local.md
*.local.txt
.todo/

# ── pi-task runtime data (local, not committed) ────────────────────────
.pi/task-session-history.json
.pi/task-registry.json
.pi/task-sessions.json
.pi/artifacts/sessions/

# ── Local planners not tracked ─────────────────────────────────────────
.wayfinder/
.taskman/
